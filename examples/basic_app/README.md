# Basic Core Usage Example

This example demonstrates how to use `shiftcraft-core` from a separate app-style folder.

## Run

```sh
mise run example-basic
```

It will read `input.json`, call `shiftcraft_core.solve`, print the schedule output, and write the result to `output.json`.

If you only want to sync dependencies:

```sh
mise run sync-all
```
