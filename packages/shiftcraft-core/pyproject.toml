[project]
name = "shiftcraft-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ortools>=9.15.6755",
]

[project.scripts]
shiftcraft-core = "shiftcraft_core:main"

[build-system]
requires = ["uv_build>=0.11.1,<0.12.0"]
build-backend = "uv_build"
